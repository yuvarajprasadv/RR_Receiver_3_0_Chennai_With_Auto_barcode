main(arguments);function main(argv){    	sourceDoc = app.activeDocument;    return SaveSourceDocAs(sourceDoc, argv[0]);}function SaveSourceDocAs (sourceDoc, docTargetPath){	try{    if ( app.documents.length > 0 )     {        var docFileName = new File ( docTargetPath );        var saveOpts = new IllustratorSaveOptions();        	saveOpts.pdfCompatible = true;        sourceDoc.saveAs(docFileName, saveOpts);        return 'Success document saved';    }    else    {        var errorString;        errorString = "Document saving failed (or) Document is not loaded";        return errorString;    }}catch(e){$.writeln(e);    return e;    }}