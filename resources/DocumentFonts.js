function getUsedFonts (doc){	try	{	     var xmlString = new XML(doc.XMPString);	     fontsInfo = xmlString.descendants("stFnt:fontName");	     var ln = fontsInfo.length(), arr = [];	     for (var i = 0; i<ln; i++){arr.push(fontsInfo[i])};	     return arr;	 }	 catch (e)	 {	 	return "Error on fetching document font list";	 }}main();    function main(){   try   {    var docFileList = getUsedFiles(activeDocument);	return docFileList;    }    catch(e)    {        return "Document is not active to fetch font. Please check UTC font server";     }}